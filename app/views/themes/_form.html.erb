<%= turbo_frame_tag "theme" do %>
	<% if success %>
		<% @css_class = success ? "form-success" : "form-error" %>
	<% end %>

	<%= form_with model: theme, data: { turbo_frame: "theme" } do |f| %>
		<%= f.text_field :name %>
		<%= f.submit "Save", class: "small" %>
	<% end %>
<% end %>
